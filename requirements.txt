# Travel AI Agent - Professional Dependencies (1M+ Users)
# Redis + PostgreSQL + Kubernetes deployment

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.6.0
python-dotenv==1.0.0

# Authentication (JWT + bcrypt)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Redis for distributed rate limiting and session management
redis>=5.0.0

# PostgreSQL for persistence
asyncpg>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.12.0

# Voice/Speech recognition
SpeechRecognition==3.10.0
pydub==0.25.1

# HTTP client for API integrations
urllib3>=2.0.0

# Optional: Voice support (Python < 3.13)
# SpeechRecognition==3.10.0
# gTTS==2.4.0
# pyaudio==0.2.13

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Linting and formatting
black==23.11.0
flake8==6.1.0
